import UserSearch from "components/UserSearch";

const HomePage = () => {
  return (
    <div className="flex flex-col container max-w-md p-6">
      <UserSearch />
    </div>
  );
};

export default HomePage;
